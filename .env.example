# Đổi tên thành .env và điền thông tin thật
PG_HOST=localhost
PG_PORT=5432
PG_DB=tnbike_db
PG_USER=postgres
PG_PASSWORD=your_password_here

# Đường dẫn dữ liệu
EML_DIR=./data/eml/
PDF_OUT_DIR=./staging/pdf/
EXTRACTED_JSONL=./staging/extracted.jsonl
ERRORS_CSV=./staging/errors.csv

# Tolerance khi so sánh tổng (VND)
TOTAL_TOLERANCE=200

# Năm tài chính của batch này
BATCH_FISCAL_YEAR=2026

# === Phase B — Dashboard LLM ===
ANTHROPIC_API_KEY=sk-ant-...